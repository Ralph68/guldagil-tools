# .github/workflows/deploy.yml
name: Déploiement FTP via git-ftp (staging & prod)

on:
  push:
    branches:
      - dev
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      # Variables communes
      DB_HOST:           ${{ secrets.DB_HOST }}
      DB_NAME:           ${{ secrets.DB_NAME }}
      DB_USER:           ${{ secrets.DB_USER }}
      DB_PASS:           ${{ secrets.DB_PASS }}
      MAIL_HOST:         ${{ secrets.MAIL_HOST }}
      MAIL_PORT:         ${{ secrets.MAIL_PORT }}
      MAIL_USERNAME:     ${{ secrets.MAIL_USERNAME }}
      MAIL_PASSWORD:     ${{ secrets.MAIL_PASSWORD }}
      MAIL_TO:           ${{ secrets.MAIL_TO }}
      # Staging
      FTP_SERVER_DEV:    ${{ secrets.FTP_SERVER_DEV }}
      FTP_USERNAME_DEV:  ${{ secrets.FTP_USERNAME_DEV }}
      FTP_PASSWORD_DEV:  ${{ secrets.FTP_PASSWORD_DEV }}
      SERVER_DIR_DEV:    ${{ secrets.SERVER_DIR_DEV }}
      # Production
      FTP_SERVER_PROD:   ${{ secrets.FTP_SERVER_PROD }}
      FTP_USERNAME_PROD: ${{ secrets.FTP_USERNAME_PROD }}
      FTP_PASSWORD_PROD: ${{ secrets.FTP_PASSWORD_PROD }}
      SERVER_DIR_PROD:   ${{ secrets.SERVER_DIR_PROD }}

    steps:
      # 1) Checkout code
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 1

      # 2) Journal local
      - name: Générer un journal de déploiement local
        if: always()
        run: |
          echo "🔁 Déploiement sur ${GITHUB_REF#refs/heads/} à $(date) par ${{ github.actor }} (commit: ${{ github.sha }})" \
            > deploy-log.txt

      # 3) Générer .env pour la BDD (reste en local)
      - name: Générer .env pour la BDD
        run: |
          cat <<EOF > .env
          DB_HOST="${DB_HOST}"
          DB_NAME="${DB_NAME}"
          DB_USER="${DB_USER}"
          DB_PASS="${DB_PASS}"
          DB_CHARSET="utf8mb4"
          APP_ENV="production"
          EOF

      # 4) Installer git-ftp
      - name: Installer git-ftp
        run: sudo apt-get update && sudo apt-get install -y git-ftp

      # 5) Préparer .git-ftp-ignore
      - name: Générer .git-ftp-ignore
        run: |
          cat <<EOF > .git-ftp-ignore
          *
          !.git-ftp-ignore
          !lib/**
          !public/**
          !config.php
          public/assets/img/**
          EOF

      # 6) Committer .git-ftp-ignore (évite le « dirty repo »)
      - name: Committer .git-ftp-ignore
        run: |
          git config user.email "actions@github.com"
          git config user.name  "GitHub Actions"
          git add .git-ftp-ignore
          git commit -m "ci: ajout patterns git-ftp ignore" --no-verify || echo "Rien à committer"

      # 7) Déployer via git-ftp (staging ou prod selon la branche)
      - name: Déployer via git-ftp
        run: |
          BRANCH=${GITHUB_REF#refs/heads/}

          if [ "$BRANCH" = "main" ]; then
            URL="ftp://${FTP_SERVER_PROD}/${SERVER_DIR_PROD}"
            USER="${FTP_USERNAME_PROD}"
            PASS="${FTP_PASSWORD_PROD}"
          else
            URL="ftp://${FTP_SERVER_DEV}/${SERVER_DIR_DEV}"
            USER="${FTP_USERNAME_DEV}"
            PASS="${FTP_PASSWORD_DEV}"
          fi

          echo "→ Déploiement sur $BRANCH : $URL"

          export GIT_FTP_URL="$URL"
          export GIT_FTP_USER="$USER"
          export GIT_FTP_PASSWORD="$PASS"

          git ftp push --auto-init --verbose

      # 8) Notification email (succès)
      - name: Envoi d’un email (succès)
        if: success()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ env.MAIL_HOST }}
          server_port:    ${{ env.MAIL_PORT }}
          username:       ${{ env.MAIL_USERNAME }}
          password:       ${{ env.MAIL_PASSWORD }}
          subject:        "✅ Déploiement ${GITHUB_REF#refs/heads/} réussi"
          body: |
            Le déploiement de ${{ github.repository }} sur la branche ${GITHUB_REF#refs/heads/} a réussi.
            Voir l’action : https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
          to:   ${{ env.MAIL_TO }}
          from: GitHub Deploy Bot <${{ env.MAIL_USERNAME }}>

      # 9) Notification email (échec)
      - name: Envoi d’un email (échec)
        if: failure()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ env.MAIL_HOST }}
          server_port:    ${{ env.MAIL_PORT }}
          username:       ${{ env.MAIL_USERNAME }}
          password:       ${{ env.MAIL_PASSWORD }}
          subject:        "❌ Échec du déploiement ${GITHUB_REF#refs/heads/}"
          body: |
            Le déploiement de ${{ github.repository }} sur la branche ${GITHUB_REF#refs/heads/} a échoué.
            Voir l’action : https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
          to:   ${{ env.MAIL_TO }}
          from: GitHub Deploy Bot <${{ env.MAIL_USERNAME }}>

